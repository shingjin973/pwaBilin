export enum State{
  Canceled = 0,
  Active = 1,
  Completed = 2
}

export enum View {
  Grid = 1,
  Table = 2
}

export enum ReviewType {
  Teacher = 1,
  Course = 2
}

export enum Levels {
  Beginner = 1,
  Intermediate = 2,
  Advanced = 3
}
